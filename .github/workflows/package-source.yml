name: Package source archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    name: Package repository source
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bulk import in progress; automatic packaging is temporarily paused on this branch."
