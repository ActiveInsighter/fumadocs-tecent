---
title: "button.tsx：统一按钮原语"
description: "用 CVA 组合语义变体与尺寸，并通过 Radix Slot 支持 Link 等多态按钮外观。"
---

<Callout type="idea" title="文件定位">

Button 是几乎所有交互组件的基础视觉原语。它只负责语义元素、状态样式和尺寸，不负责 loading、确认或网络请求。

</Callout>

## 这个文件负责什么

- 提供六种视觉 variant。
- 提供文本与三种图标尺寸。
- 继承所有原生 button 属性。
- 统一 disabled、focus-visible 与 aria-invalid 样式。
- 支持 asChild。
- 导出 variants 供 Pagination 等复用。

## 执行思路

1. variant/size 进入 CVA。
2. cn 合并 className。
3. asChild 选择 Slot 或 button。
4. 其余事件、type、disabled 透传。

## 与其他模块的关系

| 模块 | 关系 |
| --- | --- |
| `CVA` | 变体类型与样式。 |
| `Radix Slot` | 多态组合。 |
| `LoadingButton` | 在相似样式上增加 loading。 |
| `Pagination` | 复用 buttonVariants。 |

## 导出 API

| 导出 | 类型 | 用途 |
| --- | --- | --- |
| `Button` | `component` | 基础按钮。 |
| `buttonVariants` | `style factory` | 根据 variant/size 生成类名。 |

## 组合结构

<Tabs items={['普通按钮', '链接外观', '图标按钮']}>
  <Tab>

```tsx
<Button type="submit">Save</Button>
```

  </Tab>
  <Tab>

```tsx
<Button asChild variant="outline">
  <a href="/docs">Docs</a>
</Button>
```

  </Tab>
  <Tab>

```tsx
<Button size="icon" aria-label="Delete">
  <TrashIcon />
</Button>
```

  </Tab>
</Tabs>

## 带注释源码

> 原始路径：`frontend/src/components/ui/button.tsx`。以下仅增加学习性注释，不改变程序行为。

```tsx title="button.tsx" lineNumbers
import * as React from "react"
import { Slot } from "@radix-ui/react-slot"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

// CVA 把视觉 variant 与尺寸 size 组合成类型安全的样式函数。
const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:bg-primary/90",
        destructive:
          "bg-destructive text-white hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60",
        outline:
          "border bg-background shadow-xs hover:bg-accent hover:text-accent-foreground dark:bg-input/30 dark:border-input dark:hover:bg-input/50",
        secondary:
          "bg-secondary text-secondary-foreground hover:bg-secondary/80",
        ghost:
          "hover:bg-accent hover:text-accent-foreground dark:hover:bg-accent/50",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-9 px-4 py-2 has-[>svg]:px-3",
        sm: "h-8 rounded-md gap-1.5 px-3 has-[>svg]:px-2.5",
        lg: "h-10 rounded-md px-6 has-[>svg]:px-4",
        icon: "size-9",
        "icon-sm": "size-8",
        "icon-lg": "size-10",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)

// Button 保留全部原生 button 属性，并额外支持 variant、size、asChild。
function Button({
  className,
  variant,
  size,
  asChild = false,
  ...props
}: React.ComponentProps<"button"> &
  VariantProps<typeof buttonVariants> & {
    asChild?: boolean
  }) {
  // asChild 避免在 Link 外再嵌套 button，最终语义由唯一子元素提供。
  const Comp = asChild ? Slot : "button"

  return (
    <Comp
      data-slot="button"
      className={cn(buttonVariants({ variant, size, className }))}
      {...props}
    />
  )
}

export { Button, buttonVariants }
```

## 阅读重点

- 表单内按钮默认 type 是 submit；非提交按钮必须显式 `type="button"`。
- 只有图标的按钮必须提供 aria-label 或 sr-only 文本。
- asChild 不会自动改变可访问语义，子元素必须本身可交互。
- disabled 与 aria-disabled 语义不同；链接通常需要自行处理。

## Twoslash：组合两个变体维度

```ts twoslash
type Variant = 'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link'
type Size = 'default' | 'sm' | 'lg' | 'icon' | 'icon-sm' | 'icon-lg'

type ButtonOptions = { variant?: Variant; size?: Size }

const options = { variant: 'outline', size: 'sm' } satisfies ButtonOptions
//    ^?
```
