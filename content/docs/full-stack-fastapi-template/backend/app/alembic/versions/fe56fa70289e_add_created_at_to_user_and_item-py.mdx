---
title: "fe56fa70：添加 created_at 时间字段"
description: "为 User 与 Item 增加带时区的创建时间列，为排序、审计和统计提供基础。"
---

<Callout type="idea" title="迁移定位">

该 revision 为两张核心表增加创建时间。字段暂时允许为空，因此旧数据无需立即回填也能完成迁移。

</Callout>

## 版本关系

| 字段 | 值 |
| --- | --- |
| 当前 revision | `fe56fa70289e` |
| 父 revision | `1a31ce608336` |
| 执行方向 | `upgrade()` 向前，`downgrade()` 回退 |

## Upgrade 的结构效果

- 为 `item` 添加 `created_at TIMESTAMP WITH TIME ZONE`。
- 为 `user` 添加同类型的 `created_at`。
- 字段允许为空，降低对历史数据的升级阻力。
- 回滚时按相反顺序删除两个字段。

<Callout type="idea" title="为什么先允许为空">

对已有表直接新增非空列通常需要默认值或全表回填。先添加可空列、回填数据、再单独收紧为非空，是更容易控制的分阶段迁移方式。

</Callout>

## 带注释源码

> 原始路径：`backend/app/alembic/versions/fe56fa70289e_add_created_at_to_user_and_item.py`。注释用于解释迁移意图，执行语句保持不变。

```python title="fe56fa70289e_add_created_at_to_user_and_item.py" lineNumbers
"""Add created_at to User and Item

Revision ID: fe56fa70289e
Revises: 1a31ce608336
Create Date: 2026-01-23 15:50:37.171462

"""
from alembic import op
import sqlalchemy as sa
import sqlmodel.sql.sqltypes


# revision identifiers, used by Alembic.
revision = 'fe56fa70289e'
down_revision = '1a31ce608336'
branch_labels = None
depends_on = None


def upgrade():
    """为核心实体增加可空的带时区创建时间。"""
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column('item', sa.Column('created_at', sa.DateTime(timezone=True), nullable=True))  # [!code highlight]
    op.add_column('user', sa.Column('created_at', sa.DateTime(timezone=True), nullable=True))
    # ### end Alembic commands ###


def downgrade():
    """删除创建时间列；已经记录的时间值会随列永久丢失。"""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column('user', 'created_at')
    op.drop_column('item', 'created_at')
    # ### end Alembic commands ###
```

## 迁移审查重点

- 允许为空意味着旧记录和异常写入可能没有时间，需要应用层默认值或后续回填。
- 数据库列本身未设置 server default，创建时间目前主要依赖模型默认值。
- 跨时区系统应保存带时区的 UTC 时间，显示时再转换。
- 若未来改为非空，应先完整回填历史行。
