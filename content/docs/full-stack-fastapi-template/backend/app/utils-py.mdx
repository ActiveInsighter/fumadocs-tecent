---
title: "utils.py：邮件与密码重置工具"
description: "渲染 Jinja 邮件模板、发送 SMTP 邮件，并生成和验证有时效的密码重置 JWT。"
---

<Callout type="idea" title="文件定位">

这是应用级通用工具模块。它把模板渲染、SMTP 组装和密码重置令牌从路由中拆出，使接口层只负责编排。

</Callout>

## 这个文件解决什么问题

- 从构建后的 HTML 文件读取并渲染模板。
- 根据 TLS/SSL 和认证配置组装 SMTP 参数。
- 用数据类统一返回邮件正文与主题。
- 使用带 `exp`、`nbf`、`sub` 的 JWT 作为重置令牌。

## 核心执行流程

1. 业务函数选择模板并准备上下文。
2. Jinja 渲染得到 HTML。
3. `send_email` 根据 Settings 创建并发送消息。
4. 重置流程生成短期 JWT。
5. 验证函数解码令牌并返回邮箱主体。

## 与其他模块的关系

- **`core/config.py`**：提供 SMTP、项目名、前端地址和 Token 有效期。
- **邮件模板目录**：提供构建后的 HTML。
- **`routes/login.py`**：编排找回与重置密码。
- **`routes/users.py`**：发送新账户通知。

## 带注释源码

> 原始路径：`backend/app/utils.py`。以下仅增加学习性注释，不改变程序逻辑。

```python title="utils.py" lineNumbers
import logging
from dataclasses import dataclass
from datetime import UTC, datetime, timedelta
from pathlib import Path
from typing import Any

import emails
import jwt
from jinja2 import Template
from jwt.exceptions import InvalidTokenError

from app.core import security
from app.core.config import settings

# 模块级日志记录发送结果；生产中通常由应用统一配置日志格式。
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


@dataclass
class EmailData:
    """把邮件主题与 HTML 正文作为一个值对象传递。"""
    html_content: str
    subject: str


def render_email_template(*, template_name: str, context: dict[str, Any]) -> str:
    """读取已构建 HTML 模板，并用受控上下文渲染。"""
    template_str = (
        Path(__file__).parent / "email-templates" / "build" / template_name
    ).read_text()
    html_content = Template(template_str).render(context)
    return html_content


def send_email(
    *,
    email_to: str,
    subject: str = "",
    html_content: str = "",
) -> None:
    """根据 Settings 中的 SMTP 参数发送 HTML 邮件。"""
    assert settings.emails_enabled, "no provided configuration for email variables"
    assert settings.EMAILS_FROM_EMAIL  # For type checker
    message = emails.message.Message(
        subject=subject,
        html=html_content,
        mail_from=(settings.EMAILS_FROM_NAME, settings.EMAILS_FROM_EMAIL),
    )
    # 从最小必需参数开始，再按配置逐步加入 TLS/SSL 和认证。
    smtp_options = {"host": settings.SMTP_HOST, "port": settings.SMTP_PORT}
    if settings.SMTP_TLS:
        smtp_options["tls"] = True
    elif settings.SMTP_SSL:
        smtp_options["ssl"] = True
    if settings.SMTP_USER:
        smtp_options["user"] = settings.SMTP_USER
    if settings.SMTP_PASSWORD:
        smtp_options["password"] = settings.SMTP_PASSWORD
    response = message.send(to=email_to, smtp=smtp_options)
    logger.info(f"send email result: {response}")


def generate_test_email(email_to: str) -> EmailData:
    """生成验证 SMTP 和模板链路的测试邮件。"""
    project_name = settings.PROJECT_NAME
    subject = f"{project_name} - Test email"
    html_content = render_email_template(
        template_name="test_email.html",
        context={"project_name": settings.PROJECT_NAME, "email": email_to},
    )
    return EmailData(html_content=html_content, subject=subject)


def generate_reset_password_email(email_to: str, email: str, token: str) -> EmailData:
    """生成包含短期重置链接的密码恢复邮件。"""
    project_name = settings.PROJECT_NAME
    subject = f"{project_name} - Password recovery for user {email}"
    link = f"{settings.FRONTEND_HOST}/reset-password?token={token}"
    html_content = render_email_template(
        template_name="reset_password.html",
        context={
            "project_name": settings.PROJECT_NAME,
            "username": email,
            "email": email_to,
            "valid_hours": settings.EMAIL_RESET_TOKEN_EXPIRE_HOURS,
            "link": link,
        },
    )
    return EmailData(html_content=html_content, subject=subject)


def generate_new_account_email(
    email_to: str, username: str, password: str
) -> EmailData:
    """生成管理员创建账户后的初始凭据通知邮件。"""
    project_name = settings.PROJECT_NAME
    subject = f"{project_name} - New account for user {username}"
    html_content = render_email_template(
        template_name="new_account.html",
        context={
            "project_name": settings.PROJECT_NAME,
            "username": username,
            "password": password,
            "email": email_to,
            "link": settings.FRONTEND_HOST,
        },
    )
    return EmailData(html_content=html_content, subject=subject)


def generate_password_reset_token(email: str) -> str:
    """签发仅用于密码重置的短期 JWT。"""
    delta = timedelta(hours=settings.EMAIL_RESET_TOKEN_EXPIRE_HOURS)
    now = datetime.now(UTC)
    expires = now + delta
    exp = expires.timestamp()
    encoded_jwt = jwt.encode(
        # exp 限制有效期，nbf 防止提前使用，sub 保存目标邮箱。
        {"exp": exp, "nbf": now, "sub": email},  # [!code focus]
        settings.SECRET_KEY,
        algorithm=security.ALGORITHM,
    )
    return encoded_jwt


def verify_password_reset_token(token: str) -> str | None:
    """验证签名与时间声明，成功时返回邮箱，失败时返回 None。"""
    try:
        decoded_token = jwt.decode(
            token, settings.SECRET_KEY, algorithms=[security.ALGORITHM]
        )
        return str(decoded_token["sub"])
    except InvalidTokenError:
        return None
```

## 阅读重点

- 模板路径应来自受控代码而不是用户任意输入。
- 日志不要记录 Token、密码或完整敏感邮件内容。
- `assert` 更适合开发期不变量；生产关键校验可考虑显式异常。
