---
title: "deps.py：依赖注入与身份认证"
description: "为路由提供数据库会话、Bearer Token、当前用户和超级用户权限依赖。"
---

<Callout type="idea" title="文件定位">

这是 API 层最关键的横切模块。它把数据库资源生命周期与认证授权流程封装成类型别名，使每个路由函数只需声明依赖。

</Callout>

## 这个文件解决什么问题

- 每个请求创建并自动关闭 SQLModel Session。
- 从 Authorization Header 提取 Bearer Token。
- 验证 JWT、解析主体并加载用户。
- 统一检查用户存在、激活状态和超级用户权限。

## 核心执行流程

1. `SessionDep` 调用生成器创建会话。
2. `TokenDep` 使用 OAuth2PasswordBearer 提取 Token。
3. `get_current_user` 解码 JWT 并校验载荷。
4. 按 `sub` 查询用户并检查状态。
5. 需要管理员权限的路由继续执行超级用户依赖。

## 与其他模块的关系

- **`core/db.py`**：提供数据库引擎。
- **`core/security.py`**：提供 JWT 算法。
- **`models.py`**：提供 TokenPayload 与 User。
- **所有 routes**：通过类型别名复用会话和当前用户。

## 带注释源码

> 原始路径：`backend/app/api/deps.py`。以下仅增加学习性注释，不改变程序逻辑。

```python title="deps.py" lineNumbers
from collections.abc import Generator
from typing import Annotated

import jwt
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from jwt.exceptions import InvalidTokenError
from pydantic import ValidationError
from sqlmodel import Session

from app.core import security
from app.core.config import settings
from app.core.db import engine
from app.models import TokenPayload, User

# OpenAPI 会据此展示 OAuth2 密码流；运行时从 Bearer Header 取 Token。
reusable_oauth2 = OAuth2PasswordBearer(
    tokenUrl=f"{settings.API_V1_STR}/login/access-token"
)


def get_db() -> Generator[Session]:
    """为一次请求提供数据库会话，并在请求结束后自动关闭。"""
    with Session(engine) as session:
        yield session


# 用 Annotated 同时保留静态类型和 FastAPI 依赖元数据。
SessionDep = Annotated[Session, Depends(get_db)]  # [!code highlight]
TokenDep = Annotated[str, Depends(reusable_oauth2)]


def get_current_user(session: SessionDep, token: TokenDep) -> User:
    """验证访问令牌并返回当前有效用户。"""
    try:
        payload = jwt.decode(
            token, settings.SECRET_KEY, algorithms=[security.ALGORITHM]
        )
        # Pydantic 再次验证 JWT 载荷结构，避免直接信任字典。
        token_data = TokenPayload(**payload)
    except InvalidTokenError, ValidationError:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="Could not validate credentials",
        )
    user = session.get(User, token_data.sub)
    if not user:
        raise HTTPException(status_code=404, detail="User not found")
    if not user.is_active:
        raise HTTPException(status_code=400, detail="Inactive user")
    return user


# 路由声明 `current_user: CurrentUser` 即可复用整条认证链。
CurrentUser = Annotated[User, Depends(get_current_user)]  # [!code focus]


def get_current_active_superuser(current_user: CurrentUser) -> User:
    """在已认证用户基础上继续执行角色授权。"""
    if not current_user.is_superuser:
        raise HTTPException(
            status_code=403, detail="The user doesn't have enough privileges"
        )
    return current_user
```

## 阅读重点

- `yield` 依赖适合管理需要释放的资源。
- 认证回答“你是谁”，授权回答“你能做什么”。
- 失败状态码和错误信息应避免泄露过多内部信息。
