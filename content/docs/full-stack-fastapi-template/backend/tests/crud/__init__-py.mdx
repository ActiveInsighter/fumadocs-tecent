---
title: "crud/__init__.py：CRUD 测试包边界"
description: "说明直接数据访问测试与 HTTP 路由测试的分工。"
---

<Callout type="idea" title="包边界">

`tests.crud` 是测试代码的 Python 包。这个 `__init__.py` 刻意保持为空，只负责稳定包识别与导入路径，不在导入阶段执行测试准备、数据库连接或 fixture 注册。

</Callout>

## 为什么保留空文件

- 明确该目录是可导入的 Python 包，便于 Pytest、编辑器和静态分析工具解析绝对导入。
- 避免把共享 fixture 隐藏在包初始化中；共享 fixture 应放在 `conftest.py`。
- 避免导入测试包时产生数据库写入、网络请求或环境变量修改等副作用。

## 目录内的重要内容

<Cards>
  <Card href="./test_user-py" title="test_user.py">直接测试用户创建、认证、更新与哈希升级。</Card>
</Cards>

## 源码

```python title="__init__.py" lineNumbers
# 仅声明 Python 包边界；测试初始化逻辑不应写在这里。
```

<Callout type="warn" title="保持导入可预测">

CRUD 测试可以绕过 HTTP，但仍应使用隔离的测试数据库和事务清理。

</Callout>
