---
title: "routes/__init__.py：路由测试包边界"
description: "说明路由测试目录如何按端点模块拆分。"
---

<Callout type="idea" title="包边界">

`tests.api.routes` 是测试代码的 Python 包。这个 `__init__.py` 刻意保持为空，只负责稳定包识别与导入路径，不在导入阶段执行测试准备、数据库连接或 fixture 注册。

</Callout>

## 为什么保留空文件

- 明确该目录是可导入的 Python 包，便于 Pytest、编辑器和静态分析工具解析绝对导入。
- 避免把共享 fixture 隐藏在包初始化中；共享 fixture 应放在 `conftest.py`。
- 避免导入测试包时产生数据库写入、网络请求或环境变量修改等副作用。

## 目录内的重要内容

<Cards>
  <Card href="./test_items-py" title="test_items.py">Item CRUD、资源归属与权限边界。</Card>
  <Card href="./test_login-py" title="test_login.py">登录、令牌、密码恢复与哈希升级。</Card>
  <Card href="./test_users-py" title="test_users.py">用户资料、注册、管理与删除权限。</Card>
  <Card href="./test_private-py" title="test_private.py">仅测试环境开放的私有辅助端点。</Card>
</Cards>

## 源码

```python title="__init__.py" lineNumbers
# 仅声明 Python 包边界；测试初始化逻辑不应写在这里。
```

<Callout type="warn" title="保持导入可预测">

测试模块应可以独立收集；不要依赖某个测试文件先运行。共享准备工作放进 fixture 或 `tests/utils`。

</Callout>
