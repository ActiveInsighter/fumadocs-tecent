---
title: Fumadocs MDX 综合语法示例
description: 展示 Fumadocs 支持的 Markdown、MDX 组件、代码与类型表格语法。
icon: BookOpen
---

import { Home, Rocket, Settings, TriangleAlert } from 'lucide-react';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Accordions, Accordion } from 'fumadocs-ui/components/accordion';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { Files, Folder, File } from 'fumadocs-ui/components/files';
import { TypeTable } from 'fumadocs-ui/components/type-table';

# Fumadocs MDX 综合语法示例

这是一篇用于验证文档站能力的示例页面，涵盖常见 Markdown 语法和 Fumadocs UI 组件。

## 基础文本

普通文本可以包含 **粗体**、*斜体*、~~删除线~~ 和 `行内代码`。

> 这是一个引用块。引用中也可以使用 **强调** 和 `代码`。

### 列表

- 无序列表项目
- 另一个项目
  - 嵌套项目

1. 安装依赖
2. 编写文档
3. 发布站点

### 任务列表

- [x] 创建 Fumadocs 项目
- [x] 配置 MDX
- [ ] 补充业务文档

## 链接、表格与代码

- [文档首页](/docs)
- [Fumadocs 官网](https://fumadocs.dev)

| 属性 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `title` | `string` | 无 | 页面标题 |
| `enabled` | `boolean` | `true` | 是否启用 |
| `count` | `number` | `0` | 项目数量 |

```tsx title="components/mdx.tsx"
import defaultMdxComponents from 'fumadocs-ui/mdx';

export function getMDXComponents(components = {}) {
  return { ...defaultMdxComponents, ...components };
}
```

## Cards 导航卡片

<Cards>
  <Card title="返回首页" href="/" icon={<Home />}>
    返回网站首页。
  </Card>
  <Card title="快速开始" href="/docs/what-is-fumadocs" icon={<Rocket />}>
    了解 Fumadocs 的基本概念。
  </Card>
  <Card title="配置说明" href="/docs/comparisons" icon={<Settings />}>
    查看相关方案对比。
  </Card>
  <Card title="无链接卡片">
    `href` 属性是可选的。
  </Card>
</Cards>

## Callout 提示块

<Callout title="提示" type="info">
  使用 Callout 突出说明、注意事项和操作结果。
</Callout>

<Callout title="注意" type="warn" icon={<TriangleAlert />}>
  修改重要配置前，请先确认部署环境和环境变量。
</Callout>

<Callout title="完成" type="success">
  这是一条成功状态提示。
</Callout>

## Tabs 标签页

<Tabs items={['npm', 'pnpm', 'bun']} defaultIndex={1}>
  <Tab value="npm">使用 `npm install` 安装依赖。</Tab>
  <Tab value="pnpm">使用 `pnpm install` 安装依赖。</Tab>
  <Tab value="bun">使用 `bun install` 安装依赖。</Tab>
</Tabs>

## Accordion 折叠面板

<Accordions type="single">
  <Accordion title="Fumadocs 支持自定义 MDX 组件吗？">
    支持。React 组件可以注册到 MDX 组件映射中，然后直接在文档里使用。
  </Accordion>
  <Accordion title="可以修改站点主题吗？">
    可以通过 CSS 变量、Tailwind CSS 和组件样式覆盖默认主题。
  </Accordion>
</Accordions>

<Accordions type="multiple">
  <Accordion title="第一个折叠项目">第一个项目的内容。</Accordion>
  <Accordion title="第二个折叠项目">第二个项目的内容。</Accordion>
</Accordions>

## Steps 步骤组件

<Steps>
  <Step>
    ### 安装依赖

    安装项目所需的软件包。
  </Step>
  <Step>
    ### 编写文档

    在 `content/docs` 目录中创建 `.mdx` 文件。
  </Step>
  <Step>
    ### 推送发布

    将变更推送到仓库，由 CI/CD 发布到 Vercel。
  </Step>
</Steps>

## Files 文件结构

<Files>
  <Folder name="app" defaultOpen>
    <Folder name="docs" defaultOpen>
      <File name="layout.tsx" />
      <File name="page.tsx" />
    </Folder>
  </Folder>
  <Folder name="content" defaultOpen>
    <Folder name="docs" defaultOpen>
      <File name="index.mdx" />
      <File name="mdx-syntax-example.mdx" />
      <File name="meta.json" />
    </Folder>
  </Folder>
  <File name="source.config.ts" />
</Files>

## TypeTable 类型表格

<TypeTable
  type={{
    title: {
      description: '组件标题',
      type: 'string',
      required: true,
    },
    enabled: {
      description: '是否启用组件',
      type: 'boolean',
      default: true,
    },
    count: {
      description: '显示的项目数量',
      type: 'number',
      default: 10,
    },
  }}
/>

## HTML 与 JSX

<div className="rounded-lg border p-4">
  这是一个直接写在 MDX 中的 JSX 容器。
</div>

键盘快捷键：<kbd>Ctrl</kbd> + <kbd>K</kbd> 打开搜索，按 <kbd>Esc</kbd> 关闭弹窗。

## Frontmatter 字段

- `title`：页面标题。
- `description`：页面描述，也会用于 SEO。
- `icon`：页面树中的图标名称。

## 总结

<Callout title="示例完成" type="success">
  如果本页中的 Markdown、Cards、Callout、Tabs、Accordion、Steps、Files 和 TypeTable 均能正常显示，说明基础 MDX 能力已经配置成功。
</Callout>
